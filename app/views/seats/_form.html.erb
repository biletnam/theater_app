<%= form_for(@seat) do |f| %>
  <% if @seat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@seat.errors.count, "error") %> prohibited this seat from being saved:</h2>

      <ul>
      <% @seat.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :row %><br />
    <%= f.text_area :row %>
  </div>
  <div class="field">
    <%= f.label :chair_letter %><br />
    <%= f.text_area :chair_letter %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>